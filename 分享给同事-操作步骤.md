# 把工具分享给同事使用 — 操作步骤

任选下面一种方式即可。

---

## 方式一：同一办公室 / 同一 WiFi（局域网分享）

**适合**：同事和你在同一网络（公司内网、家里 WiFi 等），你电脑开着、工具在运行时他们才能用。

### 你的操作

1. **先关掉当前正在跑的开发服务器**（若还在运行）：在 Cursor 终端里按 **Ctrl+C** 停掉。

2. **用「允许局域网访问」的方式启动**：在 Cursor 里打开终端（**Ctrl+`**），执行：
   ```bash
   cd "/Users/chenxiu/Downloads/AI chenxiu"
   npm run dev:share
   ```
   看到 `Ready on http://0.0.0.0:3000` 或 `Local: http://localhost:3000` 就说明启动成功。

3. **查本机 IP 地址**：
   - 点击 Mac 左上角 **苹果菜单 → 系统设置 → 网络 → 当前连接（Wi‑Fi 或以太网）**，可以看到 IP，形如 `192.168.1.100`；
   - 或在终端执行：`ifconfig | grep "inet "`，看 `192.168.x.x` 或 `10.x.x.x` 那个。

4. **把链接发给同事**：  
   链接格式：**http://你的IP:3000**  
   例如：`http://192.168.1.100:3000`  
   同事用浏览器打开这个链接即可使用。

5. **同事使用**：打开链接后，在页面顶部配置栏**各自填写自己的 Gemini API Key**，即可使用。Key 只存在各自浏览器，不会存到你电脑。

**注意**：你的电脑不能关机、不能关掉终端；若公司网络有防火墙，可能需开放 3000 端口。

---

## 方式二：部署成公网链接（推荐，一次部署长期用）

**适合**：同事不在同一网络、或希望有一个固定链接随时打开。

### 你的操作

1. **把代码推到 GitHub**（若还没推过）：
   - 在 [github.com/new](https://github.com/new) 新建一个仓库（如名字填 `ai-tool`）；
   - 在 Cursor 终端执行（把 `你的GitHub用户名` 换成你的）：
   ```bash
   cd "/Users/chenxiu/Downloads/AI chenxiu"
   git init
   git add .
   git commit -m "init"
   git branch -M main
   git remote add origin https://github.com/你的GitHub用户名/ai-tool.git
   git push -u origin main
   ```
   若提示登录，用 GitHub 账号或 Personal Access Token。

2. **在 Vercel 部署**：
   - 打开 [vercel.com](https://vercel.com)，用 **GitHub** 登录；
   - 点 **Add New… → Project**，选择刚推送的仓库，点 **Import** → **Deploy**；
   - 等 1～2 分钟，页面会显示 **Visit** 或 **Domains**，例如：`https://ai-tool-xxxx.vercel.app`。

3. **把该链接发给同事**：  
   同事用浏览器打开即可，每人填自己的 API Key 使用。

详细步骤可看项目里的 **《部署到Vercel拿到链接.md》**。

---

## 小结

| 方式     | 你做什么                    | 发给同事的链接              |
|----------|-----------------------------|-----------------------------|
| 方式一   | 运行 `npm run dev:share`，查本机 IP | `http://你的IP:3000`        |
| 方式二   | 推 GitHub + Vercel 部署     | `https://xxx.vercel.app`    |

同事拿到链接后：**用浏览器打开 → 在配置栏填自己的 Gemini API Key** 即可使用，无需再装别的。
