# 部署成公网链接 — 按这 3 步做

本地 Git 已就绪，你只需完成下面 3 步即可拿到 https://xxx.vercel.app 链接。

---

## 第 1 步：在 GitHub 新建仓库

1. 打开 **https://github.com/new**
2. **Repository name** 填：`ai-tool`（或任意英文名）
3. 选 **Public**，**不要**勾选 “Add a README file”
4. 点 **Create repository**
5. 记下仓库地址，形如：`https://github.com/你的用户名/ai-tool.git`

---

## 第 2 步：在 Cursor 终端推送代码（按顺序做）

### 2.1 先确认你的 GitHub 用户名

- 打开 https://github.com 并登录
- 右上角点你的头像，你的**用户名**就是头像下面那一行英文（例如 `chenxiu`）
- 记下这个英文名，下面会用到

### 2.2 打开 Cursor 里的终端

- 在 Cursor 里按键盘：**Ctrl+反引号**（反引号 \` 在数字 1 左边，和 ~ 在一起）
- 或者菜单：**终端(Terminal)** → **新建终端**
- 底部会出现一个黑色/深色窗口，里面有光标在闪，那就是终端

### 2.3 输入第一条命令（进入项目文件夹）

在终端里**复制粘贴**下面这一整行，然后按 **回车**：

```bash
cd "/Users/chenxiu/Downloads/AI chenxiu"
```

- 一般不会报错，光标会换一行，可以继续输入下一条

### 2.4 输入第二条命令（把本地和 GitHub 仓库连起来）

把下面这一行里的 **你的GitHub用户名** 六个字，改成你在 2.1 记下的英文用户名（例如改成 `chenxiu`），再复制粘贴到终端，按 **回车**：

```bash
git remote add origin https://github.com/你的GitHub用户名/ai-tool.git
```

- 如果仓库名不是 `ai-tool`，把 `ai-tool` 改成你在第 1 步建的仓库名

### 2.5 输入第三条命令（把代码推送到 GitHub）

复制下面这一行，粘贴到终端，按 **回车**：

```bash
git push -u origin main
```

- **可能会弹出登录**：选「用浏览器登录」或输入 GitHub 用户名+密码；若要求填密码时，密码可能要用 **Personal Access Token**（见下方说明）
- **成功时**：终端里会出现 `Writing objects: 100%`、`Branch 'main' set up to track...` 之类的字样，没有红色报错就表示第二步完成

---

**若提示 `remote origin already exists`**：先执行下面这行，再重新做 2.4 和 2.5：

```bash
git remote remove origin
```

**若要求输入密码**：GitHub 现在可能不允许多次用账号密码，需要改用 **Token**：
1. 打开 https://github.com/settings/tokens → **Generate new token (classic)**
2. 勾选 **repo**，生成后**复制**那一串字符（只显示一次）
3. 在终端里再次执行 `git push -u origin main`，密码那里**粘贴这一串 Token**（不会显示出来，正常粘贴后回车即可）

---

## 第 3 步：在 Vercel 部署

1. 打开 **https://vercel.com**，用 **GitHub** 登录
2. 点 **Add New…** → **Project**
3. 在列表里选刚推送的 **ai-tool**，点 **Import**
4. 直接点 **Deploy**，等 1～2 分钟
5. 页面会显示 **Visit** 或 **Domains**，例如：**https://ai-tool-xxxx.vercel.app**

**这个链接就是公网链接**，复制发给同事即可，他们用浏览器打开、填自己的 API Key 就能用。
